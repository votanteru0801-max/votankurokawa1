# テスト用: docker compose -f docker-compose.yml -f docker-compose.test.yml up -d db
# その後 alembic upgrade head && pytest を実行する。
services:
  db:
    environment:
      POSTGRES_DB: kuroeda_test
    ports:
      - "5433:5432"
